# AI Provider Configuration
# Choose ONE provider and uncomment/set the appropriate API key:

# Option 1: Anthropic Claude (recommended)
CLAUDE_AI_SESSION_KEY=your-claude-api-key-here

# Option 2: OpenAI
# OPENAI_API_KEY=your-openai-api-key-here

# Gateway Token (generated during setup with 'docker compose run --rm openclaw-cli dashboard --no-open')
OPENCLAW_GATEWAY_TOKEN=

# ============================================
# Optional: Twingate Configuration
# Only required if enabling remote/team access
# ============================================

# Your Twingate network name (without .twingate.com)
# Example: If your Twingate URL is "acme.twingate.com", use "acme"
TWINGATE_NETWORK=

# Twingate Connector tokens (generate in Admin Console)
TWINGATE_ACCESS_TOKEN=
TWINGATE_REFRESH_TOKEN=
